/* * * Functionality for highlight button * * */

/* Turn on clicks*/

const lumafield = document.querySelector("#Lumafield");
lumafield.addEventListener("click", (event)=> {
   // To Do: Need to select all the cards, then loop through them to check the 
  }
});
